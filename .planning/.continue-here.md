---
context: superpowers-writing-plans
phase: D2-sla-order-list-column
task: writing-plans (orientation+advisor done; plan NOT yet written)
total_tasks: 12 (proposed; not yet committed to file)
status: paused
last_updated: 2026-05-27T22:00:00Z
branch: feat/order-status-processing-time
worktree: C:/tmp/accu-mk1-wave1
head: 23a0840
---

# Resume D2 plan-writing (paused at 96% context, before plan file written)

<current_state>
Mid-execution of `superpowers:writing-plans` for **D2** (SLA order-list column + per-tier amber threshold). Spec finalized + pushed (`23a0840`). Orientation reads done. Advisor consulted — gave 7 sharpening points (all listed below). Paused BEFORE writing `docs/superpowers/plans/2026-05-27-d2-sla-column.md` because writing the full ~12-task plan would have risked context cutoff mid-file. Everything needed to write the plan is captured in `.planning/HANDOFF.json` and below.
</current_state>

<completed_work>
- Sub-project A (SLA tier model + endpoints): shipped through `43cc693`.
- Sub-project C (SlaPane + service-group tier dropdown): shipped through `43cc693`.
- Sub-project B (business-hours engine + calendar + batch endpoint + UI): fully complete, all 11 tasks reviewed + final holistic review READY-TO-MERGE through `c8841d2`. Spec at `docs/superpowers/specs/2026-05-27-sla-business-hours-design.md`, plan at `docs/superpowers/plans/2026-05-27-sla-business-hours.md`.
- D2 brainstorming: complete, design approved by user.
- D2 spec written, self-reviewed, and pushed: `docs/superpowers/specs/2026-05-27-d2-sla-column-design.md` (commits `96f2917` → `3658ba3` → `23a0840`).
- D2 writing-plans orientation: backend (SamplePriority model, existing bulk IN pattern, /analysis-services local vs proxied), frontend (OrderRow + OrderStatusPage data root + card-view goalNote site, sampleLookupMap construction) — all pinned.
- D2 advisor consult: 7 sharpening points captured (see "Advisor sharpenings" below).
</completed_work>

<remaining_work>
1. **Write D2 plan** to `docs/superpowers/plans/2026-05-27-d2-sla-column.md` (~12 tasks; B's plan is the structural template). Bake in the 7 advisor sharpenings.
2. Self-review the plan; commit + push.
3. Offer execution mode (subagent-driven recommended, matching B).
4. Execute D2 (~12 tasks with per-task two-stage review, same flow as B).
5. After D2: ship decision (the integration menu — `superpowers:finishing-a-development-branch`).
</remaining_work>

<decisions_made>
All locked in `.planning/HANDOFF.json` (`decisions_locked_for_d2`). Highlights:
- New SLA column on `OrderRow` (table view) + REPLACE card-view hardcoded 24/48h goalNote (NOT enhance existing Lab time).
- Full tier fidelity: `priority > group-tier > default`; multi-group sample → tightest group tier.
- Worst-active-sample drives order verdict; published excluded; not-received → "awaiting".
- Clock = per-sample `date_received` (check-in); matches D1 Lab line.
- Amber threshold is **per-tier** (`sla_tiers.amber_threshold_percent INTEGER NOT NULL DEFAULT 20`, 1–100).
- **`POST /sample-priorities/lookup`** with `{sample_uids: [...]}` body (NOT GET — advisor caught 500-UID URL-length issue), cap 500, sparse response `{items:[{sample_uid,priority}]}`.
- TanStack Query: 5-min stale, sorted-UID hash key. Scaling-out path documented but deferred.
- No live ticking; refetch with orders query + Refresh button.
</decisions_made>

<blockers>
None — pause was purely context budget, not a blocker.
</blockers>

## Required Reading (in order)
1. `docs/superpowers/specs/2026-05-27-d2-sla-column-design.md` — the D2 contract (commits 96f2917 → 23a0840).
2. `docs/superpowers/plans/2026-05-27-sla-business-hours.md` — B's plan, the structural template (header, TDD bite-sized steps, complete code blocks, per-task verify + commit + push, Docker/restart gotchas).
3. `.planning/HANDOFF.json` — the structured handoff with proposed_plan_tasks + key_orientation_facts.
4. `docs/superpowers/handoffs/2026-05-27-sla-feature.md` (untracked from prior session) — operational gotchas (worktree path, Docker test paths, restart needs, lint baseline, Playwright degradation).

## Advisor sharpenings (BAKE INTO THE D2 PLAN)
1. **`POST /sample-priorities/lookup`** (DONE in spec — write plan to match).
2. **i18n keys co-commit with the consuming task** (B's pattern). `orderStatus.sla.*` with the OrderSlaCell task; `preferences.sla.amberThreshold`/`percentRemaining` with the SlaPane task. Do NOT consolidate into a late "i18n" task.
3. **`useOrderSlaStatuses` queryKey MUST be a stable hash** — `['order-sla-status', sortedUidsHash, sortedReceivedAtHash, tierConfigHash]`. Otherwise re-renders thrash the 5-min stale window.
4. **Verdict aggregation is `useMemo`, not another `useQuery`** — pure transform of cached inputs + the one snapshot `/sla/status` query.
5. **Two cell components**: `OrderSlaCell` (order-aggregated, table view) + `SampleSlaIndicator` (per-sample, card-view goalNote replacement) sharing a color-classification primitive.
6. **Named test**: `test_unmapped_analysis_keyword_falls_through_to_default` in `resolveSampleTier` suite.
7. **Task 12 (verification sweep) heads-up**: the 2 pre-existing dev-DB tier-drift failures (`test_default_tier_encodes_old_24h_goal`, `test_list_returns_seeded_default`) are KNOWN out-of-scope; do not bisect D2 looking for them.

## Critical Anti-Patterns (do NOT repeat)
| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `ON CONFLICT DO NOTHING` ≠ "won't re-add deleted rows" | Discovered in B (sub-project): `ON CONFLICT DO NOTHING` only no-ops on rows that still exist; a deleted row gets re-inserted. The "delete-to-disable + survives restart" guarantee comes from the **first-boot-only** seeder gated by a `settings` flag, NOT from `ON CONFLICT`. Spec for B claimed otherwise; corrected during plan-writing. | advisory | Any "delete to opt out" + auto-seeder design requires a tombstone OR first-boot-only gating (not just ON CONFLICT). Surface this assumption to the user during spec review. |
| Lint script doesn't scope to passed files | `npm run lint -- <files>` does NOT scope; it lints the whole project (B Task 10 verification almost flagged 119 baseline errors as "regressions"). Use `npx eslint <files>` directly for scoped checks. | advisory | Use `npx eslint <files>` (host) for scoped lint of changed files. |
| OneDrive checkout is parked on master | The default cwd is the OneDrive checkout (master). Real worktree for this feature is `C:/tmp/accu-mk1-wave1`. Docker containers bind-mount the WORKTREE. Always use absolute paths under `C:/tmp/accu-mk1-wave1` and `git -C /c/tmp/accu-mk1-wave1`. | blocking | Every subagent prompt MUST state the worktree path explicitly. |
| Backend tests in container at `/app/tests` not `backend/tests` | `docker exec accu-mk1-backend sh -c 'cd /app && python -m pytest tests/<file> -q'`. If pytest missing: `docker exec accu-mk1-backend pip install --quiet pytest`. | advisory | Standard incantation in every test-running plan step. |

## Infrastructure State
- **Branch**: `feat/order-status-processing-time`, HEAD `23a0840`, pushed.
- **Docker** (verified up at pause): `accu-mk1-backend` and `accu-mk1-frontend` running (bare names; NOT the `accumark-host-*` / `accumark-subvial-*` containers, which are different platforms per the handoff).
- **DB**: `accumark_mk1` Postgres. Has `business_hours_config`, `lab_holidays` (~33 federal seeded rows for 3 years), `sla_tiers`, `sla_priority_tiers`, `service_groups.sla_tier_id`. Pre-existing drift: default tier has `target_minutes=2880` (not the seeded 1440), and a stray `Microbiology` tier (id=21, 6720 min). May be real config or test pollution — unresolved out-of-scope.
- **Backend health**: `curl -fsS http://localhost:8012/health` → ok.
- **Backend live OpenAPI** confirmed routes for `/business-hours-config`, `/lab-holidays`, `/lab-holidays/{holiday_date}`, `/lab-holidays/generate-federal`, `/sla/status`. All auth-gated (401 without token).
- **`.planning/STATE.md`** modified (GSD artifact, leave out of feature commits — per the original handoff).

<context>
The resumed agent should re-invoke `superpowers:writing-plans` (already done once, just re-enter to start fresh in the new context). The skill prompt is in scope. Bake in the 7 advisor sharpenings while writing. Use the `proposed_plan_tasks` array from `.planning/HANDOFF.json` as the task scaffold. Each task gets B-plan-style TDD bite-sized steps with complete code blocks (no placeholders, no "similar to Task N"). Watch the lint-scope and worktree-path anti-patterns above. After writing + self-review, commit the plan doc on `feat/order-status-processing-time` and push, then offer execution mode (subagent-driven recommended).
</context>

<next_action>
1. `cd` mentally to worktree `C:/tmp/accu-mk1-wave1`.
2. Re-read the D2 spec at `docs/superpowers/specs/2026-05-27-d2-sla-column-design.md` and `.planning/HANDOFF.json`.
3. Invoke `superpowers:writing-plans` skill.
4. Write the plan to `docs/superpowers/plans/2026-05-27-d2-sla-column.md` with the 12 proposed tasks, baking in all 7 advisor sharpenings.
5. Self-review (placeholder scan, type consistency, spec coverage).
6. `git -C /c/tmp/accu-mk1-wave1 add docs/superpowers/plans/2026-05-27-d2-sla-column.md && git -C /c/tmp/accu-mk1-wave1 commit -m "docs(sla): D2 implementation plan" && git -C /c/tmp/accu-mk1-wave1 push origin feat/order-status-processing-time`.
7. Offer execution choice (recommend subagent-driven).
</next_action>
