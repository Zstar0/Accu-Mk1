---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src-tauri/tauri.conf.json, src/App.tsx]
autonomous: true
must_haves:
  truths:
    - "App runs in browser at localhost:5173"
    - "App builds as Tauri desktop application"
    - "Dev server starts without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "tauri"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri configuration"
      contains: "identifier"
    - path: "src/App.tsx"
      provides: "Main React component"
  key_links:
    - from: "npm run dev"
      to: "localhost:5173"
      via: "Vite dev server"
    - from: "npm run tauri build"
      to: "src-tauri/target"
      via: "Tauri bundler"
---

<objective>
Set up the frontend application using dannysmith/tauri-template as the base.

Purpose: Establish the dual-mode app shell (browser + Tauri) that can run independently of the backend for UI development.
Output: Working React app that runs in browser and builds as Tauri desktop app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Template reference: https://github.com/dannysmith/tauri-template
- React 19 + TypeScript + Vite 7
- Tauri v2 with Rust
- shadcn/ui v4 + Tailwind CSS v4
- Zustand + TanStack Query for state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone and set up Tauri template</name>
  <files>package.json, src-tauri/tauri.conf.json, src/</files>
  <action>
    Clone dannysmith/tauri-template into current directory (which should be empty except .planning/).

    Steps:
    1. Run: npx degit dannysmith/tauri-template . --force
    2. Run: npm install
    3. Update src-tauri/tauri.conf.json:
       - Set identifier to "com.accumark.accu-mk1"
       - Set productName to "Accu-Mk1"
       - Set version to "0.1.0"

    DO NOT modify the existing .planning/ directory.
  </action>
  <verify>
    - package.json exists with tauri dependencies
    - src-tauri/tauri.conf.json has updated identifier
    - node_modules/ exists
  </verify>
  <done>Template cloned, dependencies installed, Tauri configured for Accu-Mk1</done>
</task>

<task type="auto">
  <name>Task 2: Verify browser mode works</name>
  <files>vite.config.ts</files>
  <action>
    Start the Vite dev server and verify the app runs in browser mode.

    Steps:
    1. Run: npm run dev
    2. Wait for server to start (should be localhost:5173 or similar)
    3. Verify no critical errors in console output
    4. Kill the dev server after verification

    If port conflicts, note the actual port used.
  </action>
  <verify>
    - Dev server starts without fatal errors
    - Output shows localhost URL
  </verify>
  <done>Browser mode works, dev server runs at localhost</done>
</task>

<task type="auto">
  <name>Task 3: Verify Tauri desktop build works</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/main.rs</files>
  <action>
    Build the Tauri desktop application to verify the full stack compiles.

    Steps:
    1. Run: npm run tauri build
    2. Wait for build to complete (may take several minutes first time)
    3. Verify output binary exists in src-tauri/target/release/

    Note: This requires Rust toolchain to be installed. If build fails due to missing Rust, note the error.
  </action>
  <verify>
    - Build completes without errors
    - Binary exists at src-tauri/target/release/
  </verify>
  <done>Tauri desktop build succeeds, binary produced</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Template cloned successfully
- [ ] npm install completed without errors
- [ ] tauri.conf.json updated with Accu-Mk1 identifiers
- [ ] Dev server starts (npm run dev)
- [ ] Tauri build succeeds (npm run tauri build)
</verification>

<success_criteria>
- All tasks completed
- App runs in browser at localhost
- Tauri desktop build produces binary
- No TypeScript or Rust compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
