# Handoff: Post-1.0 Deploy + Variance/Native Fixes — paused before 1.0.2 batch

*Created 2026-06-20. Paste this into a fresh session to resume with full context.*

---

You're picking up after the **Accumark 1.0 platform release shipped to prod** and a long run of post-deploy fixes. Status: **1.0 live; a follow-up Accu-Mk1 1.0.2 batch is planned but NOT executed.** Your job is to drive whatever the user asks next — most likely **executing the 1.0.2 plan**, regenerating P-1010's COA, or validating the native sub-sample cutover.

## Working directories

| Repo / dir | Path | Branch | Latest commit |
|---|---|---|---|
| Accu-Mk1 (wave1 worktree) | `C:/tmp/accu-mk1-wave1` | `subsample-features` | `9e7a53a` (migration hardening — unbumped, NOT deployed) |
| COABuilder (worktree) | `C:/tmp/coabuilder-varcap` | `feat/variance-capable-services` | `f3eb1d3` |
| integration-service | `…/Accumark-Workspace/integration-service` | `subsample-features` | `b8c0e4a` (1.0.2, deployed) |
| accumarklabs (DevKinsta WP) | `\\wsl.localhost\…\DevKinsta\public\accumarklabs` | `subsample-features` | `a67040d` (2.27.1, deployed) |

All four merged to master via merge commits earlier. `gh` authed as **Zstar0**.

## Prod deploy state (ground truth, pulled this session)

| Service | Prod version | Notes |
|---|---|---|
| integration-service | **1.0.2** | DateSampled tz fix live |
| COA Builder | **2.27.0** | variance-capable services |
| Accu-Mk1 | **1.0.1** | ⚠️ migration hardening `9e7a53a` committed but NOT yet deployed (needs 1.0.2) |
| WordPress theme | **2.27.1** | variance launch badge + draft-dropdown fix |
| `SUBSAMPLE_NATIVE_CREATE` | **`1`** | native sub-samples ON in prod `backend/.env` |

## What's on the branch (layered)

**Layer 1 — entry state.** Came in from `2026-06-19-1.0-release-merged.md`: all 4 PRs merged to master, nothing deployed.

**Layer 2 — the 1.0 deploy.** Deployed all four in dependency order: IS 1.0.1 → COA 2.27.0 → Mk1 1.0.1 (web, `--skip-release`) → WP 2.27.0. JWT verified consistent across IS/COA/WP; caches purged; user ran the Mk1 `analysis-services/sync`.

**Layer 3 — post-deploy fixes (the bulk of this session):**
- **IS 1.0.2 — DateSampled tz fix (`b8c0e4a`, deployed).** Order→AR creation stamped `date.today().isoformat()` (bare date); SENAITE read it as midnight-UTC and rendered it as the *previous day 7:00 PM*. Now `datetime.now().astimezone()`. New orders only; existing ARs (P-1009 etc.) keep the old value.
- **WP 2.27.1 — variance launch (`a67040d`, deployed).** Launch-discount badge on the new-order wizard's Variance card (driven by the product's sale price via `get_price()`); **fix B**: Test Services admin dropdown now lists draft products so a beta add-on is linkable.
- **Variance launch config on prod (data, done):** linked ghost product **4117** to the `wc_test_services` "Variance Testing" entry, set its **type=`addon`** (was `addon-coming-soon` — this was why the card/price/badge didn't show), set **sale price $76.50** (reg $90 → "Save 15%"), tester flag on user **1544**.
- **Identity conformance + COA variance root cause = unlinked peptides.** P-1010 variance showed "ID 0 of 3 conform" AND the COA had no variance mean block — both because 87/88 identity Analysis Services on prod had `peptide_id = NULL`. **Backfilled 70 by exact name-match on prod (done).** Committed an idempotent **startup migration `9e7a53a`** to auto-link on boot (NOT deployed yet — ships with 1.0.2).
- **Native sub-sample cutover.** Flipped `SUBSAMPLE_NATIVE_CREATE=1` in prod `backend/.env` + recreated backend. New vials are Mk1-only (`mk1://` uids, no SENAITE secondary). Existing secondaries left in place (user may discard in SENAITE).
- **Cross-repo SENAITE audit (3 parallel agents).** COABuilder + IS are **safe** (parent-AR-only / variance data body-supplied). Accu-Mk1 has **one real gap**: native vials under a non-container parent are invisible in the worksheet inbox (→ fixed in the 1.0.2 plan).
- **Wrote the 1.0.2 plan** (`docs/superpowers/plans/2026-06-20-mk1-1.0.2-batch.md`) — NOT executed.

## Critical operational gotchas

| Gotcha | Why it matters | How to handle |
|---|---|---|
| **deploy.sh has CRLF + worktree repos break under WSL bash** | Mk1 + COA are git *worktrees*; running `deploy.sh` from PowerShell (→ WSL bash, `/mnt/c`) fails the git-tag step with "not a git repository". | `sed -i 's/\r$//' scripts/deploy.sh` first; run worktree deploys from **Git Bash**. Mk1: use `--skip-release` (skips tag + desktop Tauri trigger). IS is a normal clone — fine from PowerShell. |
| **WP `deploy.py` git-add landmine** | Its commit step does `git add -A` if the tree is dirty — would sweep the intentionally-untracked `accumark-hardening.php` mu-plugin into a commit. | Move the mu-plugin aside before `deploy.py`, restore after. Tree must be clean so the commit step no-ops. |
| **WP `deploy.py` health checks report "got 0"** | Looks like prod is down; it's a deploy-client false negative. | Verify prod independently with `curl` — REST 200, etc. |
| **Prod DB writes blocked by auto-mode classifier** | A bare "yes"/one-letter approval is NOT enough authorization for prod `wp eval`/SQL/`.env` writes. | Get explicit per-action authorization, or have the user run via the `!` prefix. |
| **`container_mode` is state-gated at first-touch** | `_PRE_RECEIVED_STATES` (sample_due etc.) → `container_mode=True`. The **108 sample_due backlog is SAFE** (native vials show on check-in). Risk is only after-the-fact vial adds on the **135 already-received** samples. | The 1.0.2 inbox fix makes the inbox container_mode-independent. |
| **Migration auto-link uses `lower(trim())` exact match** | Won't catch near-matches: `ID_MTP-131`("MTP 131") vs peptide "MTP131", `ID_Decapeptide-12` vs "Decapeptide - 12". | Link svc 97→#109 and 106→#125 manually (1.0.2 plan Task 5 Step 4). |
| **Mk1 prod DB query path** | No psql; managed DO Postgres. | `ssh root@165… 'docker exec -i -w /app accu-mk1-backend python -'` then use `from database import engine/SessionLocal`. |
| **DevKinsta wp-cli is broken** | Container PHP 7.4 vs theme requires 8.1+ → fatal. | Use the `devkinsta_db` MySQL container directly, or wp-admin, for local WP data. |
| **Variance product is draft** | A draft WC product is visible (with the gate) but **not purchasable** — checkout may balk. | GA decision: publishing 4117 makes it buyable AND visible to everyone (flag stops mattering). |
| **Observe, don't infer (burned twice)** | The DateSampled "frontend theory" was wrong (it was IS); "prerequisites verified" missed the `wc_test_services.type` field. | Always call the actual endpoint/DB/serialization before concluding. |
| **GitNexus "index stale" advisory** | Fires on Bash calls in IS. | Noise — ignore. |

## Infrastructure state

- **Prod droplet `165.227.241.81`**: IS (:8000), COA (`coabuilder_service` :5000), Mk1 (`accu-mk1-backend`/`-frontend`), `senaite` (:8080, healthcheck shows "unhealthy" but the **API serves 200** — false alarm, long-standing). Prod env in each `/root/<svc>/.env` (manual, never overwritten; a `backend/.env.bak-native-*` backup was made this session).
- **Kinsta WP** `35.236.52.109:51209`, theme at `/www/accumarklabs_415/public/wp-content/themes/wpstar`. Deploy via `deploy.py` (tar). NEVER DB-sync.
- **Local dev**: wave1 stack — `accu-mk1-backend` (:8012, no `--reload`, restart after edits), `accu-mk1-frontend` (:3101, no HMR across bind mount), `accumark_postgres` (:5432, DB `accumark_mk1`). **Mk1 tests run in-container**: `docker exec accu-mk1-backend python -m pytest tests/…`. Parallel `accumark-subvial-*` / `accumark-host-*` stacks exist; DevKinsta serves `accumarklabs.local`.

## Verification commands (re-run, don't trust stale numbers)

| What | Command |
|---|---|
| Prod versions | `ssh root@165.227.241.81 'for s in integration-service coabuilder accu-mk1; do printf "%-20s " $s; cat /root/$s/.deploy/current_version; done'` |
| Native flag | `ssh root@165.227.241.81 'docker exec accu-mk1-backend printenv SUBSAMPLE_NATIVE_CREATE'` (expect `1`) |
| Prod identity-service link gap | `ssh root@165… 'docker exec -i -w /app accu-mk1-backend python -' <<'PY'`<br>`from database import engine; from sqlalchemy import text`<br>`print(engine.connect().execute(text("SELECT count(*) FROM analysis_services WHERE left(keyword,3)='ID_' AND peptide_id IS NULL")).scalar())` (expect 17 until the 2 manual links land) |
| Mk1 tests | `docker exec accu-mk1-backend python -m pytest tests/ -q` (~19 pre-existing failures are baseline — verify via stash, not regressions) |
| Mk1 FE typecheck | `docker exec accu-mk1-frontend npx tsc --noEmit -p tsconfig.json` |

## Outstanding items the user may want next

1. **Execute the Accu-Mk1 1.0.2 batch** — plan at `docs/superpowers/plans/2026-06-20-mk1-1.0.2-batch.md`. 4 code tasks (inbox native-vial fix, variance-set defaults, native-create default, version bump) + deploy + 2 manual peptide links. User was about to choose **subagent-driven vs inline** execution.
2. **Regenerate P-1010's COA** — peptide now linked, so the variance mean block will render (verified `process_variance_fields` returns the series). Confirms Mk1→COABuilder→WP verify page end-to-end.
3. **Validate the native cutover** — check a vial into a **new** order; confirm `mk1://` uid, inbox visibility, assign→results→COA flow, worksheet inbox.
4. **Alias model brainstorm** — multiple Analysis Services / customer-chosen names → one peptide (substance vs display-name separation; conformance-against-alias-set; COA renders ordered name). System-wide; needs its own spec.
5. **15 remaining unlinked identity services** — depends on the alias model + catalog cleanup (incl. `Survovtudie` typo, `PNC-27`/`SS-31` missing names, TB-500/Thymosin-Beta-4 ambiguity).
6. **Variance GA decision** — publish product 4117 (→ GA for everyone + purchasable) vs keep the draft+flag beta.
7. **Refresh stale memories** — several say "1.0 unmerged/undeployed"; now deployed. Also: native sub-samples now default; variance launch config; the peptide-link gotcha.

## User collaboration preferences

- **Decisive, drive-forward.** Wants momentum + a recommendation on real forks. **Prefers prose questions** — does NOT like the AskUserQuestion tool.
- **Production changes need explicit sign-off; prod DB/.env writes need explicit per-action authorization** (the classifier blocks otherwise).
- **Additive only — don't re-architect shipped systems.** Failing tests default to "stale" (verify via `git stash` baseline).
- **Observe, don't infer** — call the real endpoint/DB/serialization; don't reason from assumptions (cost real turns twice this session).
- **Commit when asked; merge is the explicit gate.** Atomic commits with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Leaves agent tooling (`.claude/`, `AGENTS.md`, `CLAUDE.md` in IS) and `accumark-hardening.php` mu-plugin **intentionally untracked** — don't commit them.

## Recommended first action in the new session

Confirm prod state, then ask which thread to drive:
`ssh root@165.227.241.81 'for s in integration-service coabuilder accu-mk1; do printf "%-20s " $s; cat /root/$s/.deploy/current_version; done; docker exec accu-mk1-backend printenv SUBSAMPLE_NATIVE_CREATE'`
— expect IS `1.0.2`, COA `2.27.0`, Mk1 `1.0.1`, native `1`. Then ask: **execute the 1.0.2 plan (subagent-driven vs inline), regenerate P-1010's COA, or validate the native cutover?**
