# Handoff: Accumark 1.0 — merged to master, awaiting deploy

*Created 2026-06-19. Paste this into a fresh session to resume with full context.*

---

You're picking up the **Accumark 1.0 Platform Release**. Status: **all 4 PRs MERGED to master; NOT yet deployed.** This session drove the final bug fixes + cosmetics, committed/pushed everything, and merged the PRs (the deploy gate, which the user explicitly authorized). Your job is to drive whatever the user asks next — most likely **running the deploy** + the post-deploy checklist, or refreshing now-stale memories.

## Working directories

| Repo / dir | Path | Branch | Latest branch commit | master merge commit |
|---|---|---|---|---|
| Accu-Mk1 (wave1 worktree) | `C:/tmp/accu-mk1-wave1` | `subsample-features` | `3f75169` | `564341c` (PR #10) |
| COABuilder | `C:/tmp/coabuilder-varcap` | `feat/variance-capable-services` | `f3eb1d3` | `7c310d6` (PR #1) |
| integration-service | `C:/Users/forre/OneDrive/Documents/GitHub/Accumark-Workspace/integration-service` | `subsample-features` | `5a105fb` | `6f3b2cc` (PR #7) |
| accumarklabs (DevKinsta WP) | `\\wsl.localhost\…\DevKinsta\public\accumarklabs` | `subsample-features` | `5a022a6` | `8fb8d36` (PR #5) |

All branches pushed + in sync with origin. `gh` authed as **Zstar0** (works for both Zstar0 + ValenceAnalytical repos). Merges were `--merge` (merge commits — atomic history preserved); feature branches kept (worktrees track them).

## What's on the branch (this session, layered)

**Layer 1 — entry state.** Came in from `2026-06-18-variance-1.0-release.md`: the RC was built across 4 repos with PRs OPEN, nothing merged. Variance-capable Analysis Services + subvial features already on the branches.

**Layer 2 — bug fixes + small features (all on Mk1 `subsample-features` unless noted):**
- **Endotoxin NMT (COABuilder `f3eb1d3`)** — `parse_endotoxin` used strict `<`; a result *exactly at* the limit (5.0 default / 0.25 BW) failed. Switched to `<=` and the COA spec display to `≤ N EU/mL`. Boundary tests added.
- **Sterility/PCR sub-sample dropdown** — sub-sample selection inputs rendered free-text instead of a dropdown. Root cause: `analysis_services.result_options` was never seeded (the parent reads SENAITE live, masking it). Fix is DATA, not code: `POST /analysis-services/sync`. No commit — it's a post-deploy step.
- **Activity-log variance verifications (`64a5cf8`)** — variance vials terminate in `variance_verified` (never promoted) so their sign-off was invisible in the timeline. `list_variance_verifications_for_parent` derives one event/vial from the transition log (backfills retroactively); federated into `get_sample_activity` + FE icon/level.
- **Variance overlay identity conformance (`444d6b7`)** — "Computed across selected" miscounted identity (HPLC-ID `"1"` float-parsed → numeric; ID_* peptide-name values counted as non-conforming). `kind` now keys off `result_options` presence; `identity_conforms()` mirrors COABuilder `_identity_matches`; `_categorical_stats` tallies an explicit per-result `conforms` flag.
- **Parent un-promote on retest (`64a5cf8` + `78561ca`)** — retesting a parent AR reset the source vial but left the parent's promoted value stale. `cascade_parent_retest_to_sources` now retracts the *verified* parent AND nulls its `result_value` (the display filters by `retest_of_id`, not state, so retract-only wasn't enough). Never touches a `published` parent. COA-safe: `_LIVE_RESULT_STATES` excludes `retracted`.

**Layer 3 — cosmetics + release mechanics:**
- **Official assignment-role colours (`b5d4407`)** — centralised the palette (was copy-pasted in 8 files) into `src/lib/assignment-colors.ts`. HPLC→green, ENDO→orange, PCR(`ster`)→purple, EXTRA(`xtra`)→light blue. Analysis-row vials now carry their role colour; variance keeps the blue Layers icon.
- **Print-labels selection fix (`89f4934`)** — unchecked labels still printed (2 selected → 9 pages). `.label-row{display:contents}` out-ordered `.label-row-unchecked{display:none}` at equal specificity; fixed by reordering + `.print-area` qualifier.
- **STER→PCR rename + micro sub-chip colours (`3f75169`)** — `ster` label → "PCR" across 8 sites (print label too; dashboard single-letter → "P"); worksheets Microbiology sub-chips: Endotoxin→orange, Sterility→purple.
- **Post-deploy checklist (`ac12780`)** — `docs/superpowers/handoffs/2026-06-19-1.0-post-deploy-checklist.md` (tracked, on the PR).
- Merged all 4 PRs to master.

## Critical operational gotchas

| Gotcha | Why it matters | How to handle |
|---|---|---|
| **Parent DETAILS page renders SENAITE, not Mk1** | Mk1 un-promote fixes the Mk1 row + COA, but the parent-details retest display still shows the stale value (SENAITE leaves the old result on the new `unassigned` analysis). Burned ~10 turns inferring the wrong layer. | DEFERRED to SENAITE removal. Always OBSERVE the FE data source (call the serialization), don't infer. Memory `project_parent_unpromote_on_retest`. |
| **Mk1 schema + `variance_capable` seed auto-run on startup** | `_run_migrations()` runs idempotent ALTER TABLEs + the BW `variance_capable` UPDATE (database.py ~751-761) on every boot. `result_options` column is auto but stays EMPTY. | Don't run the BW seed by hand. DO run `POST /analysis-services/sync` for `result_options`. |
| **`list_analyses_for_host(include_retests=False)` returns the SUPERSEDED row** | Filters `retest_of_id IS NULL` (the documented anti-pattern). Display-only — the parent overlay uses `include_retests=True`. **COA consumers are SAFE** (variance_series uses `retested.is_(False)`; source_resolver is parent-tier). | Latent; not fixed. Memory `architecture_retest_current_row_idiom`. |
| **~19 Mk1 backend tests fail pre-existing** | Shared-live-DB + env-flaky (clickup/sla/state-machine path tests). Verified NOT regressions via `git stash` baseline. | Don't chase them. All variance/coa/identity/un-promote suites green. |
| **`gh pr merge` prints nothing on success** | Looked like a no-op; merges actually succeeded. | Verify via `gh pr view N --json state` (→ MERGED). |
| **COABuilder `:5000` topology** | Docker now shows `coabuilder_service` (baked image) on :5000, not the temp `coabuilder-varcap-live`. The endotoxin fix is merged but may not be live on the dev :5000. | Post-deploy: build+deploy the real 2.27.0 image; revert recipe in the checklist + memory `architecture_coabuilder_container_topology`. |
| **GitNexus "index stale" advisory fires every Bash call** | Noise. | Ignore. |

## Infrastructure state

- **Wave1 dev stack** (what this session tested against): `accu-mk1-backend` :8012 (no `--reload` — `docker restart` after BE edits), `accu-mk1-frontend` :3101 (Vite, no HMR across bind mount — restart after FE edits; was restarted for every cosmetic change), `accumark_postgres` :5432 (DBs `accumark_mk1`, `accumark_integration`). Bind-mounts `C:/tmp/accu-mk1-wave1`.
- **`coabuilder_service`** :5000 (Up 7h, baked image). **integration-service** runs in the workspace IS worktree (no `--reload` — restart after edits).
- **Parallel stacks (do not confuse):** `accumark-subvial-*` (:55xx) and `accumark-host-*` (:55xx) — both up, healthy. Not touched this session.
- **DevKinsta WP** `accumarklabs.local` (`devkinsta_fpm` PHP 8.2 for `php -l`). Theme served live, no build.
- **Production deploy is manual** — `deploy.sh`/`deploy.py`; merging did NOT auto-deploy. IS alembic does NOT auto-run (no new migrations this release).

## Verification commands (re-run, don't trust stale numbers)

| What | Command |
|---|---|
| All 4 PRs merged | `for r in Zstar0/Accu-Mk1 ValenceAnalytical/coabuilder ValenceAnalytical/accumark-integration-service Zstar0/accumarklabs; do gh pr list -R $r --state merged --limit 1; done` |
| Mk1 variance/identity/un-promote suites | `docker exec accu-mk1-backend python -m pytest tests/test_lims_analyses_service.py -k "unpromote or variance_verif" tests/test_variance_stats.py -q` |
| Mk1 FE typecheck | `docker exec accu-mk1-frontend npx tsc --noEmit -p tsconfig.json` |
| COABuilder endotoxin | `cd /c/tmp/coabuilder-varcap && python -m pytest tests/test_addon_parsing.py -q` |
| result_options seeded (after sync) | `docker exec accumark_postgres psql -U postgres -d accumark_mk1 -tA -c "SELECT keyword, result_options FROM analysis_services WHERE result_type='select';"` |

## Outstanding items the user may want next

1. **Deploy 1.0** — `accumark-deploy` skill (order + JWT). IS is in the change set; must be redeployed + restarted (no `--reload`).
2. **Run the post-deploy checklist** — 3 required manual steps: Mk1 `POST /analysis-services/sync`, WP COA result-breakdown backfill (recreate `_backfill-coa-specs.php`), COABuilder real-image deploy. Plus confirms (Variance shadow WC product, JWT). Doc: `docs/superpowers/handoffs/2026-06-19-1.0-post-deploy-checklist.md`.
3. **Refresh stale memories** — several still say "unmerged feature branches / unpushed / merge is the gate" (`project_variance_capable_services`, `project_variance_bucket_assignment_shipped`, etc.). Now merged + released.
4. **Version bumps** if wanted — Mk1 → 1.0.2, theme → 2.27.0 (COABuilder already 2.27.0, IS 1.0.1).
5. **Endotoxin** — already-published COAs with a result exactly at the limit need a per-COA **regen** to flip to Pass (on demand, not bulk).
6. **Deferred / upcoming (not this release):** SENAITE-backed parent-details retest display (memory `project_parent_unpromote_on_retest`); spec/validation engine migration out of COABuilder into Mk1 Analysis Services (memory `project_spec_validation_engine_migration`).

## User collaboration preferences

- **Decisive, drive-forward** — wants momentum; surface genuine forks with a recommendation. Rejected the AskUserQuestion tool repeatedly — **prefers prose questions**.
- **Commit when asked, push when asked, merge is the explicit gate** (now passed). Atomic commits, `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` trailer.
- **Additive-only; don't re-architect** shipped systems. Failing tests default to "stale," verified via stash baseline.
- **Observe, don't infer** — on display bugs, call the actual serialization/endpoint; the advisor flagged this twice before I listened.
- **Verify against domain truth** (e.g. endotoxin NMT, BW all-in-range were lab decisions). Use the `advisor` before committing to an approach.
- Did NOT auto-commit agent tooling (`.claude/`, `AGENTS.md`, `CLAUDE.md` in IS) or the manual `accumark-hardening.php` mu-plugin — left untracked by design.

## Recommended first action in the new session

Confirm release state, then ask whether to run the deploy:
`for r in Zstar0/Accu-Mk1 ValenceAnalytical/coabuilder ValenceAnalytical/accumark-integration-service Zstar0/accumarklabs; do gh api repos/$r/commits/master --jq '.sha[0:9]+"  "+.commit.message' | head -1; done` — expect the 4 merge commits (`564341c`/`7c310d6`/`6f3b2cc`/`8fb8d36`). Then ask: run the 1.0 deploy (`accumark-deploy` skill + post-deploy checklist), or something else?
