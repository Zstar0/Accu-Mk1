# Docker LOCAL build — loaded when building for local Docker testing
# Use relative /api path since Nginx proxies to the backend container
# For production builds, use: --build-arg ENV_FILE=.env.docker.prod
VITE_API_URL=/api
VITE_WORDPRESS_URL=https://accumarklabs.local
VITE_SENAITE_URL=http://localhost:8080/senaite
