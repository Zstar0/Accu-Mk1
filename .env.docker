# Docker production build — loaded when building for Docker deployment
# Use relative /api path since Nginx proxies to the backend container
VITE_API_URL=/api
VITE_WORDPRESS_URL=https://accumarklabs.com
VITE_SENAITE_URL=https://senaite.valenceanalytical.com
